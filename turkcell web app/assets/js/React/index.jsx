var Buttons = require('Buttons');


const Index = (props) => {

    return (
        <div>
            <Buttons/>
        </div>

    );
};

ReactDOM.render(<Person />, document.getElementById('root'));
